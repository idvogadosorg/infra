apiVersion: v1
kind: Service
metadata:
  name: {{ include "idvogados.fullname" . }}
  labels:
    {{- include "idvogados.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idvogados.selectorLabels" . | nindent 4 }}
