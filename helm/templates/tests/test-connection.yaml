apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idvogados.fullname" . }}-test-connection"
  labels:
    {{- include "idvogados.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idvogados.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
